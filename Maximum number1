#include<stdio.h>
 
int main()
{   
    int input,i,max;
     
     
    for(i = 0; i<3; i++)
    {
        printf(" Number #%d : ", i+1);
        scanf("%d", &input);
         
        if(i==0 || input > max)
        {
            max = input;
        }
    }
     
    printf("\n The maximum is %d\n\n", max);
     
    return 0;
}
